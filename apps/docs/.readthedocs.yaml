version: 2

build:
    os: ubuntu-lts-latest
    tools:
        python: latest
    jobs:
        pre_create_environment:
            - asdf plugin add uv
            - asdf install uv latest
            - asdf global uv latest

        create_environment:
            - cd apps/docs && uv venv

        install:
            - cd apps/docs && uv sync

        build:
            html:
                - cd apps/docs && uv run zensical build --clean

        post_build:
            - mkdir -p $READTHEDOCS_OUTPUT/html/
            - cd apps/docs && cp --recursive site/* $READTHEDOCS_OUTPUT/html/